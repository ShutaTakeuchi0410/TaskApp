/ ログイン後のヘッダー
= link_to 'ホーム', home_path 
= link_to '検索'
= link_to '追加', new_task_path
= link_to 'プロジェクト追加', new_project_path
= link_to '分析'
= current_user.email

hr

h4 プロジェクト一覧
- current_user.projects.each do |project|
  = link_to project.name, project_path(project.id)
  br

hr