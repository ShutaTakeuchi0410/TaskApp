/ ログイン後のヘッダー
= link_to 'ホーム', home_path, class: "btn btn-secondary"
= link_to '検索', '', class: "btn btn-secondary"
= link_to '追加', new_task_path, class: "btn btn-secondary"
= link_to 'プロジェクト追加', new_project_path, class: "btn btn-secondary"
= link_to '分析', '', class: "btn btn-secondary"
= current_user.email

hr

h4 プロジェクト一覧
- current_user.projects.each do |project|
  / バリデーションが実行されても１レコード分空欄ができてしまうから、表示させないようにしている
  - break if project.id == nil
  = link_to project.name, project_path(project.id)
  br

hr

= link_to 'インボックス', home_inbox_path, class: "btn btn-info"
= link_to '今日のタスク', home_path, class: "btn btn-info"
= link_to '今後のタスク', home_future_path, class: "btn btn-info"

hr
