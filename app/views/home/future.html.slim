= render :partial => "layouts/templates/header"

h1 今後のタスク

/ タスク一覧
table.table
  thead
  tr
    td タスク
    td コメント
    td タグ
    td プロジェクト
    td 締切日
  - @tasks.each do |task|
    tbody
      tr
        / タスク名
        th = link_to task.title, task_path(task)

        / コメント
        td = task.comments.size

        / タグ
        - if task.tags.present?
          td
            - task.tags.each do |tag|
             = link_to tag.name, tag_path(tag.id)
             br
        - else 
          td なし
        
        / プロジェクト
        - if task.project.present?
          td = link_to task.project.name, project_path(task.project.id)
        - else 
          td なし

        / 締切日
        - if task.deadline.present?
          td = task.deadline.strftime('%-m/%-d')
        - else 
          td なし